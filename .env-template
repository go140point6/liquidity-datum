########################################
# Discord Bot Configuration
########################################

# Discord bot token (REQUIRED)
# From: Discord Developer Portal → Bot → Token
BOT_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# Discord application (client) ID (REQUIRED)
# From: Discord Developer Portal → General Information → Application ID
CLIENT_ID=YOUR_DISCORD_APPLICATION_ID_HERE

# Discord guild (server) ID for slash command registration (REQUIRED)
# Enable Developer Mode in Discord → right-click server → Copy ID
GUILD_ID=YOUR_DISCORD_GUILD_ID_HERE


########################################
# Datum DB
########################################

# DEV:
DATUM_DB_PATH=./data/liquidity-datum-dev.sqlite
# PROD:
#DATUM_DB_PATH=./data/liquidity-datum.sqlite

# Sentinel DB (read-only attach)
# DEV:
SENTINEL_DB_PATH=/full/path/to/liquidity-sentinel-dev/data/liquidity-sentinel-dev.sqlite
# PROD:
#SENTINEL_DB_PATH=/full/path/to/liquidity-sentinel/data/liquidity-sentinel.sqlite


########################################
# Logging
########################################

# Logging verbosity:
# 0 = errors only
# 1 = warnings + errors
# 2 = info + warnings + errors
# 3 = debug + info + warnings + errors
DEBUG=2
# Disable colors (CI, log files)
# NO_COLOR=1
# Force colors (Docker / piped output)
# FORCE_COLOR=1


########################################
# Scanner / RPC (Datum)
########################################

# Try a dedicated public RPC for Datum scans
DATUM_FLR_SCAN_RPC=https://flare.rpc.thirdweb.com
DATUM_FLR_SCAN_BLOCKS=500
DATUM_FLR_SCAN_PAUSE_MS=500
DATUM_SCAN_OVERLAP_BLOCKS=50
DATUM_DATA_STALE_MINUTES=30


########################################
# Scheduler / Example Jobs (unused)
########################################